{
  "name": "Token",
  "prefix": "secretManager",
  "description": "The following example us prefixes all secrets with `staging-`.",
  "config": {
    "project.id": "123454234",
    "secret.prefix": "staging-"
  },
  "connectorConfig": {
    "username": "${secretManager:mysql-connector:username}",
    "password": "${secretManager:mysql-connector:password}"
  },
  "tip": "Some of the settings of this config provider can be configured via environment variables."
}